## Commands:

```
terraform init
```

```
terraform plan -var bucket_name="YOUR UNIQUE BUCKET NAME" -var region_name="YOUR PREFER REGION NAME" -var profile_name="YOUR AWS PROFILE NAME" -var owner_tag="YOUR OWNER TAG" -var project_tag="YOUR PROJECT NAME"
```

```
terraform apply -var bucket_name="YOUR UNIQUE BUCKET NAME" -var region_name="YOUR PREFER REGION NAME" -var profile_name="YOUR AWS PROFILE NAME" -var owner_tag="YOUR OWNER TAG" -var project_tag="YOUR PROJECT NAME"
```
